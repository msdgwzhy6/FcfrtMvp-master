package ${packageName}.mvp.${activityClassModule}.contract;

import com.fcfrt.baselib.base.IFcfrtBaseView;
import com.fcfrt.baselib.base.IFcfrtBasePresenter;

/**
 * 项目名称：
 * 类名称：${ContractName}
 * 类描述：${activityClass}Activity的Contract
 * 作者：${activityClassAuthor}
 * 创建时间：
 * 邮箱：
 * 修改简介：
 */
public interface ${ContractName} {

    interface ${IPresenterName} extends IFcfrtBasePresenter {

    }
    interface ${IViewName} extends IFcfrtBaseView {

    }
}
